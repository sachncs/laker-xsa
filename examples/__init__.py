"""Examples for LAKER-XSA.

Each module is self-contained and can be invoked as
``python -m examples.<module>``.

The deprecated v1 :class:`FusedXSALAKERAttention` is selected via
``attention_type="fused"``; the current v2 :class:`LakerAttention` via
``attention_type="fused_v2"``.
"""
